#include<stdio.h>
int main()
{
    //1
    // int a,b,divi,re;
    // scanf("%d %d", &a, &b);
    // divi=a/b;
    // re=a-(divi*b);
    // printf("%d", re);

    //3
    int n,i;
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++){
        scanf("%d", &arr[i]);
    }
    if(a)

    //4
    // int score;
    // do{
    // scanf("%d", &score);
    // }while(score<0);
    // if(score>=0 && score<=30){
    //     printf("Danger! You need to do a lot of practice");
    // }else if(score>=30.5 && score<=41){
    //     printf("Hmm, you need to do some practice");
    // }else if(score>=41.5 && score<=59.5){
    //     printf("Not bad, just do a bit more practice");
    // }else{
    //     printf("Good work! Please continue doing your practice");
    // }
    return 0;
}